from setuptools import setup, find_packages

setup(
    name='ambiguous_parsing',
    packages=find_packages("src"),
    package_dir={"": "src"},
    version="1.0",
    install_requires=[
    ],
    description="",
    author=""
)
